<div class="container">
  <h2 class="font-weight-bold bg-warning text-white p-2 mt-4">編輯餐廳資訊</h2>
  <form action="/restaurants/{{{restaurant.id}}}" method="POST" class="p-3 mb-3 border border-info">
    <div class="form-row">
      <div class="form-group col-md-6">
        <label for="name">餐廳名稱</label>
        <input type="text" class="form-control" id="name" placeholder="名稱" name="name" value="{{{restaurant.name}}}"
          required>
        <div class="invalid-feedback">
          請輸入餐廳名稱
        </div>
      </div>

      <div class="form-group col-md-6">
        <label for="name_en">餐廳英文名</label>
        <input type="text" class="form-control" id="name_en" placeholder="英文名 (optional)" name="name_en"
          value="{{{restaurant.name_en}}}">
      </div>
    </div>
    <div class="form-row">
      <div class="form-group col-md-4">
        <label for="category">餐廳類別</label>
        <select id="category" class="form-selection" name="category" required>
          <option {{{setSelected restaurant.category '美式'}}}>美式</option>
          <option {{{setSelected restaurant.category '義式餐廳'}}}>義式餐廳</option>
          <option {{{setSelected restaurant.category '日式料理'}}}>日式料理</option>
          <option {{{setSelected restaurant.category '中東料理'}}}>中東料理</option>
          <option {{{setSelected restaurant.category '火鍋'}}}>火鍋</option>
          <option {{{setSelected restaurant.category '咖啡'}}}>咖啡</option>
          <option {{{setSelected restaurant.category '中式料理'}}}>中式料理</option>
          <option {{{setSelected restaurant.category '小吃'}}}>小吃</option>
          <option {{{setSelected restaurant.category '其他'}}}>其他</option>
        </select>
        <div class="invalid-feedback">
          請選擇一個類別
        </div>
      </div>
    </div>
    <div class="form-group">
      <label for="image">餐廳照片網址</label>
      <input type="text" class="form-control" id="image" placeholder="https://... (optional)" name="image"
        value="{{{restaurant.image}}}">
    </div>
    <div class="form-group">
      <label for="location">餐廳地址</label>
      <input type="text" class="form-control" id="location" placeholder="地址" name="location"
        value="{{{restaurant.location}}}" required>
      <div class="invalid-feedback">
        請輸入餐廳地址
      </div>
    </div>
    <div class="form-group">
      <label for="phone">電話</label>
      <input type="text" class="form-control" id="phone" placeholder="電話" name="phone" value="{{{restaurant.phone}}}"
        required>
      <div class="invalid-feedback">
        請輸入餐廳電話
      </div>
    </div>

    <div class="form-group">
      <label for="google_map">餐廳 Google Map 網址</label>
      <input type="text" class="form-control" id="google_map" placeholder="https://... (optional)" name="google_map"
        value="{{{restaurant.google_map}}}">
    </div>

    <div class="form-row">
      <div class="form-group col-md-4">
        <label for="rating">評分</label>
        <input type="number" class="form-control" id="rating" placeholder="請輸入評分" name="rating" step="0.1" min="0"
          max="5" value="{{{restaurant.rating}}}">
      </div>
    </div>

    <div class="form-group">
      <label for="description">餐廳敘述</label>
      <textarea class="form-control" id="description" placeholder="簡單敘述" name="description"
        value="{{{restaurant.description}}}" rows="3" required>{{{restaurant.description}}}</textarea>
      <div class="invalid-feedback">
        請輸入餐廳簡述，推薦菜色等等
      </div>
    </div>

    <button class="btn btn-primary" type="submit">Save edit</button>


  </form>
</div>